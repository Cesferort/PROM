<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Dialogos" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#6CCD70</item>
        <item name="colorPrimaryVariant">#5DA558</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Color personalizado para el background -->
    <color name="background_color">#7fffd4</color>
    <style name="AppTheme" parent="@style/Theme.Dialogos">
        <item name="android:windowBackground">@color/background_color</item>
    </style>

    <!-- Estilo personalizado NO utilizado
     Dentro del activity tendría que escribir: style="@style/MiEstilo" en los elementos -->
    <style name="MiEstilo" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#00FF00</item>
        <item name="android:typeface">monospace</item>
        <item name="android:gravity">center</item>
    </style>
</resources>